<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Generator</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="{{ asset('js/password-form-sending.js') }}"></script>
    <link rel="stylesheet" href="{{ asset('css/base-styles.css') }}">
</head>
<body>
    <h1>Password Generator</h1>
        <form id="passwordForm">
            <div class="form-row">
                <label for="length">Password Length:</label>
                <input type="number" id="length" name="length" min="1" max="32" required>
            </div>
            <div class="form-row">
                <input type="checkbox" id="includeNumbers" name="includeNumbers">
                <label for="includeNumbers">Include Numbers</label>
            </div>
            <div class="form-row">
                <input type="checkbox" id="includeUppercase" name="includeUppercase">
                <label for="includeUppercase">Include Uppercase Letters</label>
            </div>
            <div class="form-row">
                <input type="checkbox" id="includeLowercase" name="includeLowercase">
                <label for="includeLowercase">Include Lowercase Letters</label>
            </div>
            <button type="submit">Generate Password</button>
        </form>

    <div id="passwordResult"></div>
</body>
</html>
